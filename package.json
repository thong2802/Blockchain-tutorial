{
  "name": "tokenatm_machine",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile" : "hardhat compile",
    "deploy"  : "hardhat run scripts/deploy.ts --network",
    "deployTokenPermit"  : "hardhat run scripts/deployTokenPermit.ts --network localhost",
    "test"    : "hardhat test",
    "verify"  : "hardhat verify --network"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.11.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.3",
    "dotenv": "^16.0.2"
  }
}
